import request from "supertest";
import server from "../../app";
it("responds with all post associated with a username", async () => {
  // stuff goes here
});

it("it responds with all followers associated with a username", async () => {
  // stuff goes here
});

it("it responds with all users a username is following", async () => {
  // stuff goes here
});
