import request from "supertest";
import server from "../../app";

it("responds with 201 if a when a follow has been created", async () => {
  // stuff goes here
});
it("responds with 200 if a when a follow has been deleted", async () => {
  // stuff goes here
});
